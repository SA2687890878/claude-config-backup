创建符合公司规范的 Git 分支。

分支前缀和格式规范见 `rules/git.md`。

## 执行步骤

1. **确认分支类型**
   询问用户分支用途，从 `rules/git.md` 中选择合适的前缀。

2. **生成分支名称**
   格式：`前缀/名称`
   
   规则：
   - 允许字符：字母、数字、中文、`-`、`_`
   - 使用短横线或下划线连接单词
   - 名称简洁明了，体现分支用途

3. **创建分支**
   ```bash
   git checkout -b 前缀/分支名称
   ```
