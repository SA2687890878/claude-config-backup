# 前端 UI 红线

> 违反即不合规，不接受"暂时这样"。涉及前端实现时始终生效。

## 红线
- 禁止页面 scoped 样式复制第二套通用组件视觉值（如表头背景/字号/行高/边框）——统一走共享视觉入口。
- 禁止 Dialog 嵌套 Dialog（外层弹窗内的新增/编辑再开弹窗）——改用页面内工作区或同级顺序切换。
- 禁止用可点击 `div`/`span` 代替按钮——操作项必须是真实按钮。
