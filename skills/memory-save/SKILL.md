---
name: memory-save
description: >
  保存本次会话的关键经验到持久记忆。包括：排查经验、架构决策、踩坑记录、
  最佳实践、任务进度。支持跨 session 工作恢复。
  当用户说 /memory-save、保存经验、记录经验、memory save、
  保存进度、保存记忆时触发。
version: 2.0.0
---

# Memory Save（经验积累与进度保存）

将会话中的关键经验保存到 `memory/` 目录，实现跨 session 的知识沉淀。

## 路由

| 意图 | 分支 |
|------|------|
| 保存经验/教训（"保存经验"、"记录一下"） | → A. 保存经验 |
| 保存任务进度（"保存进度"、"下次继续"） | → B. 保存进度 |
| 恢复工作（"上次做到哪了"、"继续工作"） | → C. 恢复工作 |
| 不确定 | → A. 保存经验（默认） |

---

## A. 保存经验

### 提取经验

从当前会话中提取值得记录的经验：

| 类型 | 示例 |
|------|------|
| 排查经验 | 数据库连接池耗尽、EF Core 迁移失败 |
| 架构决策 | 为什么选 A 方案而不是 B |
| 踩坑记录 | 某个 API 的隐藏行为、配置陷阱 |
| 最佳实践 | 代码模式、配置模板、命名规范 |

### 写入

**文件命名**：`memory/{kebab-case-name}.md`

**格式**：
```markdown
---
name: {short-kebab-case-slug}
description: {one-line summary}
metadata:
  type: feedback | project | reference
---

{经验内容}

**Why:** {为什么重要}
**How to apply:** {怎么用}
```

### 更新索引

在 `memory/MEMORY.md` 中添加一行索引：
```markdown
- [标题](文件名.md) — 一句话描述
```

---

## B. 保存进度

在功能开发等长流程中，保存当前进度以便下次继续。

### 写入

保存到 `memory/task-state.md`：

```markdown
---
name: task-state
description: 当前任务进度状态
metadata:
  type: project
---

## 当前任务

- **任务**: [功能名称/bug 描述]
- **阶段**: [当前在哪个 Phase]
- **下一步**: [具体要做什么]

## 已完成

- [x] Phase 1: 需求探索 — [简述结果]
- [x] Phase 2: 实现规划 — [计划文件路径]
- [ ] Phase 3: 代码实现 — 进行中

## 关键上下文

- 涉及文件：[文件列表]
- 设计决策：[关键决策]
- 已知问题：[遇到的问题]

## 恢复指令

下次会话输入"继续工作"即可恢复。
```

### 自动保存

在以下时机自动建议保存进度：
- 长流程的每个 Phase 完成时
- 用户说"先到这"、"下次继续"时
- 会话即将结束时

---

## C. 恢复工作

新会话启动时，检查 `memory/task-state.md`。

如果存在且有未完成任务：
1. 读取 `memory/task-state.md`
2. 报告上次进度："上次你在做 [任务]，完成了 [阶段]，下一步是 [下一步]"
3. 问用户："要继续吗？"

用户确认后：
- 读取相关上下文（计划文件、设计文档）
- 跳转到对应的 command/skill 继续执行
