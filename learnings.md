# 会话经验记录

> 每次会话启动时自动加载（最近 20 行）。
> 详细经验见 `rules/` 目录，按需查阅。

---

## 核心原则（每次必读）

1. 修改配置后必须运行验证（`node hook.js` 测试）
2. 不要硬编码路径，从 `input.cwd` 动态生成
3. 所有 catch 块必须有错误输出
4. readStdin 超时时 resolve 已接收数据（不是空字符串）
5. HOME 环境变量必须有 fallback（`|| ''`）

---

## 近期修改记录

